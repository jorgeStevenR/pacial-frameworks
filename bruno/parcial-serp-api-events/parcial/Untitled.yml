info:
  name: Get eventos con filtros variables
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/eventos"
  auth: inherit

runtime:
  variables:
    - name: url
      value: http://localhost:3001

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
